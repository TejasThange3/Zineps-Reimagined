import {chromium} from '@playwright/test';
const browser=await chromium.launch({channel:'chrome',headless:true});const page=await browser.newPage({viewport:{width:768,height:1000}});await page.goto('http://127.0.0.1:5173');console.log(await page.evaluate(()=>({width:document.documentElement.scrollWidth,els:[...document.querySelectorAll('body *')].filter(e=>e.getBoundingClientRect().right>innerWidth+1&&getComputedStyle(e).position!=='absolute').map(e=>({class:e.className,w:e.getBoundingClientRect().width,right:e.getBoundingClientRect().right})).slice(0,25)})));await browser.close();
